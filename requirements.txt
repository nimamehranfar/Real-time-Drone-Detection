# Drone Detection Demo (video-based) - requirements
# Install:
#   pip install -r requirements.txt
#
# Notes:
# - 'ultralytics' will pull in torch as a dependency; pip typically installs a CPU build by default.
#   If you want GPU acceleration, install the appropriate PyTorch build for your CUDA setup before (or after) this.
ultralytics>=8.2.0
opencv-python>=4.8.0
numpy>=1.24.0
PySide6>=6.6.0
